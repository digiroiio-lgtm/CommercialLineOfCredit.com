"use client";
import { FormEvent,useState } from "react";
import { pushEvent } from "./AnalyticsHooks";
export default function Calculator(){ const [result,setResult]=useState<number|null>(null); function calc(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const a=Number(f.get("amount")),r=Number(f.get("rate")),m=Number(f.get("months")); if(a<=0||r<0||m<=0){setResult(null);return;} setResult(a*(r/100)*(m/12));pushEvent("calculator_complete");} return <form className="panel form-grid" onSubmit={calc}><label>Credit amount drawn<input name="amount" type="number" min="1" step="100" defaultValue="50000" required/></label><label>Annual interest rate (%)<input name="rate" type="number" min="0" step="0.01" defaultValue="12" required/></label><label>Months outstanding<input name="months" type="number" min="1" max="120" defaultValue="6" required/></label><button className="button primary" type="submit">Estimate interest</button>{result!==null&&<div className="result">Estimated interest cost: <strong>{result.toLocaleString("en-US",{style:"currency",currency:"USD"})}</strong></div>}<p className="fine-print">Illustrative estimate only. Actual rates and costs vary by provider.</p></form>}